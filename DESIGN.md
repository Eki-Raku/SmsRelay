# SmsRelay 设计系统

SmsRelay 是一款安静、精密、可信赖的备用机短信工具。界面采用 Apple 式克制与连续性，但保留 Android 的系统返回、权限、输入法和无障碍习惯。

## 原则

- 信息层级先于装饰；颜色只表达交互、状态和风险。
- 动效用于解释因果和空间关系，不用于炫技。
- 业务页面只使用语义 token 与公共组件，不直接硬编码颜色、圆角和动画时长。
- 毛玻璃仅用于导航和模态层；卡片依靠表面色与细分隔线建立层级。
- 不使用受限的 SF Pro、SF Symbols 或 Apple Design Resources。

## Tokens

| 角色 | 浅色 | 深色 |
|---|---:|---:|
| 页面背景 | `#F5F5F7` | `#000000` |
| 主表面 | `#FFFFFF` | `#1C1C1E` |
| 次级表面 | `#EFEFF4` | `#2C2C2E` |
| 主文字 | `#1D1D1F` | `#F5F5F7` |
| 次级文字 | `#6E6E73` | `#AEAEB2` |
| 品牌交互色 | `#5E5CE6` | `#7D7AFF` |
| 成功 | `#34C759` | `#30D158` |
| 警告 | `#FF9F0A` | `#FFD60A` |
| 错误 | `#FF3B30` | `#FF453A` |

间距只使用 `4/8/12/16/20/24/32dp`。卡片圆角 `18dp`，输入和按钮 `14dp`，小型状态组件 `10dp`。文字使用 Android 系统 Sans；大标题 `32/38sp`、导航标题 `17/22sp`、正文 `16/23sp`、辅助信息 `14/20sp`、说明 `12/16sp`。

## Motion

| 场景 | 规格 |
|---|---|
| 按下 | `90ms`，缩放到 `0.98` |
| 松手 | spring，`damping=0.88 / stiffness=650` |
| 图标、颜色、开关 | `140–180ms` |
| 列表和错误提示 | `180ms`，位移不超过 `8dp` |
| 普通状态切换 | `220ms` |
| 详情页推进/返回 | `320ms`，位移不超过宽度的 `18%` |
| Sheet | spring，`damping=0.90 / stiffness=420` |

动画必须可打断。系统动画比例为零时，取消位移、缩放和动态模糊，只保留最长 `100ms` 的淡入淡出。成功和失败使用语义震动与短促图标反馈，不使用彩纸、循环呼吸或无意义弹跳。

## 公共组件

- `RelayScaffold` / `RelayTopBar`：统一安全区、标题层级和背景。
- `RelayTabBar`：全宽顶层导航；选中态只有颜色、字重、图标与一条共享光点，不允许整格填充。
- `InsetGroup` / `RelayRow`：设置与高密度列表的分组语义。
- `RelayButton` / `RelayTextField` / `RelayToggle`：必须覆盖默认、按下、焦点、禁用、执行中、成功和错误。
- `StatusPill` / `EmptyState`：只呈现状态，不承担主要操作。
- `RelaySheet`：用于新短信、确认和单一聚焦任务。

所有可点击区域至少 `48dp`，提供明确的中文 content description、角色、选中或开关状态。界面必须在浅色、深色、`2.0x` 字号、手势导航、三键导航和 `360dp` 宽度下可用。

## 禁止事项

- 不使用整格导航选中背景、大面积黑色 Hero、重复胶囊或卡片套卡片。
- 不在顶层标签切换时使用左右推屏；只有真实详情层级可以水平推进。
- 不让底栏、Sheet 或键盘遮挡内容，不以固定屏幕宽高修补安全区。
- 不把系统权限弹窗伪装成 App 自有界面，不在权限拒绝后虚假进入完成态。
