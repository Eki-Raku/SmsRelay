# 安全说明

## 先说结论

短信信使是个人维护、源码公开的实验性工具，**没有经过独立安全审计，也没有在任何应用商店上架**。它需要读取短信、成为默认短信应用，并持有邮箱 SMTP 授权码，因此一旦设备、安装包或邮箱账号失陷，可能造成短信和验证码泄露。

请只在你完全控制的备用机上使用，并自行承担安装、配置和运行风险。不要把它用于企业生产环境、关键基础设施、紧急通信，或你无法接受短信泄露与延迟的场景。

## 已知安全边界

- 应用通过 `smtp.qq.com:587` 和 STARTTLS 投递邮件，并校验证书及主机名；这不等于端到端加密。
- SMTP 授权码通过 Android Keystore 保护，但设备被解锁、Root、恶意软件入侵或系统备份行为仍可能扩大风险。
- 短信正文会保留在系统短信数据库、本地转发队列和目标邮箱中。
- 项目不承诺短信必达、实时到达或绝不重复。断网、系统省电、厂商后台限制、邮箱服务异常都可能造成延迟或失败。
- Release 仅在 GitHub Releases 发布。不要安装聊天群、网盘或第三方网站重新分发的 APK。
- 安装前请核对 Release 页面提供的 SHA-256；升级包必须保持相同签名，否则 Android 会拒绝覆盖安装。

## 建议做法

1. 为短信转发单独准备一个 QQ 发件邮箱和 SMTP 授权码。
2. 收件邮箱开启强密码和多因素认证，并定期清理不再需要的短信邮件。
3. 备用机设置锁屏密码，只安装必要应用，并保持 Android 安全更新。
4. 手机丢失、授权码疑似泄露或不再使用时，立即在 QQ 邮箱中撤销授权码。
5. 不要在 issue、日志或截图中提交真实短信、手机号、邮箱地址、授权码或设备标识。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，不要先创建公开 issue。报告中请提供受影响版本、复现步骤和影响说明，但不要附带真实凭据或他人短信内容。

当前维护版本为 `0.4.x`。个人项目不提供响应时效承诺，但会尽量确认可复现的问题。

